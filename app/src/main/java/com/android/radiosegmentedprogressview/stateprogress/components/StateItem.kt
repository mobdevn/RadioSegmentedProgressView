package com.android.radiosegmentedprogressview.stateprogress.components

class StateItem() {

   /* constructor(builder: Builder<*>) : this() {
        stateItemNumber = builder.stateItemNumber
        stateItemDescription = builder.stateItemDescription
        isCurrentState = builder.isCurrentState
        isStateChecked = builder.isStateChecked
    }

    private var stateItemNumber: StateItemNumber? = null

    private var stateItemDescription: StateItemDescription? = null

    private var isCurrentState = false

    private var isStateChecked = false


    abstract class Builder<T : Builder<T>?> : BaseItem.Builder<T>() {
        var stateItemNumber: StateItemNumber? = null
        var stateItemDescription: StateItemDescription? = null
        var isCurrentState = false
        var isStateChecked = false
        fun stateItemNumber(stateItemNumber: StateItemNumber?): T? {
            this.stateItemNumber = stateItemNumber
            return self()
        }

        fun stateItemDescription(stateItemDescription: StateItemDescription?): T? {
            this.stateItemDescription = stateItemDescription
            return self()
        }

        fun isCurrentState(isCurrentState: Boolean): T? {
            this.isCurrentState = isCurrentState
            return self()
        }

        fun isStateChecked(isStateChecked: Boolean): T? {
            this.isStateChecked = isStateChecked
            return self()
        }

        override fun build(): StateItem {
            return StateItem(this)
        }
    }


    private class Builder2 : Builder<Builder2?>() {
        override fun self(): Builder2 {
            return this
        }

        override fun BaseItem(): BaseItem {
            return BaseItem()
        }
    }

    override fun builder(): Builder<*>? {
        return Builder2()
    }


    *//*protected fun StateItem(builder: Builder<*>) {
        super(builder)

    }*//*


    fun getStateItemNumber(): StateItemNumber? {
        return stateItemNumber
    }

    fun getStateItemDescription(): StateItemDescription? {
        return stateItemDescription
    }

    fun isCurrentState(): Boolean {
        return isCurrentState
    }

    fun isStateChecked(): Boolean {
        return isStateChecked
    }*/
}